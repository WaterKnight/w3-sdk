package w3.script.gui.wtg.model.kaitai;

import io.kaitai.struct.CustomDecoder;

public class WtgFuncLookup implements CustomDecoder {
    @Override
    public byte[] decode(byte[] bytes) {
        return new byte[0];
    }
}
